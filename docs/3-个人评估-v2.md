# 个人评估

> 基于两段实习 + 多个个人项目的客观复盘，整合技能矩阵与待补齐能力。

---

## 一、实习经历

### 1.1 新华智云科技有限公司 · Java 开发实习生（2025-12 ~ 2026-05）

**项目：壮游家 CityWalk**

> 基于大模型与知识图谱构建的 AI 数字人导游系统，支持游客语音交互、AR 导览与智能故事推荐。

**技术栈**：Spring Boot · MyBatis-Plus · Redis · MySQL · Nginx · WebSocket · 大模型应用

**核心工作**：

| # | 模块 | 关键成果 |
|---|------|---------|
| 1 | 集福卡异步交付 | 三步流程架构 + 状态机解决重复弹/漏弹；DashScope 语音克隆 + Seedream 图像生成；自定义线程池 + Redis 限流 + 降级；**耗时 6s → 4s** |
| 2 | 二级 POI 缓存架构 | Caffeine（本地）+ Redis（分布式）多级缓存；Redis Pub/Sub 广播实现**多 Pod 刷新** |
| 3 | 旅行游记模块 | CompletableFuture **异步并行**处理轨迹距离、POI 补全、AI 词云；**响应时间降低 30%**；集成 LLM 实现 POI 摘要缓存 |
| 4 | 文旅投诉智能分析 | 封装**统一 AI 服务层**：实体识别、舆情分类、地域归属、场景化建议 |
| 5 | 多租户微信 JSAPI 支付重构 | 模板方法模式抽象统一下单/回调验签流程，**消除 50% 重复代码** |
| 6 | 资源管理与数据同步 | 定时任务同步微盟优惠券/商品；通用关联表管理多类型资源与景点的多对多绑定 |

📁 项目地址：`C:\Users\evil\Desktop\project\vlog-metaverse`

---

### 1.2 中国航信华东研发中心 · Java 开发实习生（2025-06 ~ 2025-09）

**项目：统一视图平台**

> 将某航空公司十几个子业务系统中的客户数据整合，构建统一的客户主数据平台；基于主数据建立智能化标签体系，支持客户画像、分群分析与精准营销。

**技术栈**：Spring Boot · MyBatis-Plus · Flink · MySQL · Redis · 海豚调度器

**核心工作**：

| # | 模块 | 关键成果 |
|---|------|---------|
| 1 | 主数据管理接口 | 客户主数据查询、客户画像标签查询、基本信息查询；敏感字段解密、掩码展示、多字段模糊匹配 |
| 2 | 端到端数据链路 | "掌尚飞"核心业务系统数据入仓与分层建模，**MySQL → ODS → DWD** 完整链路 |
| 3 | Flink 维表预加载 | 在 `MapFunction.open()` 中**预加载**机场维表到内存，避免高频点查数据库，**查询延迟降低 80%** |
| 4 | 配置信息加密 | `EnvironmentPostProcessor` 在 `ApplicationContext` 创建前**自动解密**敏感配置 |
| 5 | 日频增量同步 | 编写日频数据同步/聚合 SQL，海豚调度器**自动化调度** |
| 6 | 表设计规范 | 主导 44 张核心表命名规范与字段类型选型，完成其中 **15 张表**的 DDL 与 Java 实体类 |

---

## 二、个人项目

### 2.1 智能旅游助手 · 前后端开发

> 基于 Spring Boot + Spring AI 的智能旅游助手，提供个性化行程规划，支持多轮对话、记忆持久化、RAG 检索、联网搜索、文档生成。

**技术栈**：Spring Boot · Spring AI · RAG · Tool Calling · MCP · Vue 3

**核心亮点**：

| # | 模块 | 关键成果 |
|---|------|---------|
| 1 | 对话记忆持久化 | Kryo 序列化 + 文件系统存储；自动摘要机制压缩超阈值历史，**支持多轮对话成本可控** |
| 2 | RAG 检索优化 | `RetrievalAugmentationAdvisor` 全链路；改写 query 提升召回率；相似度阈值 + topK 控制质量；metadata 元信息注入支持**城市维度精准过滤** |
| 3 | AI 工具调用 | 基于 Spring AI 注解体系实现**联网搜索**、网页抓取、PDF 生成、资源下载 |
| 4 | 地图 MCP 集成 | STDIO 模式连接；天气查询、地址转经纬度、周边地点搜索，**支撑基于位置的推荐** |
| 5 | 知识库热更新 | 爬虫定时抓取 → 自动生成旅游知识文档 → **无需重启服务**即可检索最新信息 |

📁 项目地址：`C:\Users\evil\Desktop\project\ai-agent`

---

### 2.2 代驾通 · 后端开发

> 线上代驾服务系统（乘客端 + 司机端 + 运营后台三端协同），乘客/司机用微信小程序实现，覆盖登录认证、下单、抢单、支付、优惠卷等核心流程。

**技术栈**：Spring Boot · Redis · Redisson · RabbitMQ · MyBatis-Plus · MongoDB · XXL-Job · Docker

**核心亮点**：

| # | 模块 | 关键成果 |
|---|------|---------|
| 1 | 微信登录认证 | 调用微信接口获取 openid 判首次登录；Redis 存储 Token + UserId |
| 2 | 实时订单位置匹配 | **Redis GEO** 司机位置实时上报与检索；XXL-Job 定时调度；订单精准推送至附近司机队列 |
| 3 | 高并发抢单控制 | Redis 幂等 + Redisson 分布式锁 + DB 乐观锁**三层防护**，防止超卖与重复提交 |
| 4 | 司乘同显与轨迹 | 接驾阶段 Redis 实时同步；订单开始后 **MongoDB 持久化轨迹**；双方调用地图服务动态同步路线 |
| 5 | 多线程异步编排 | CompletableFuture 异步改造订单结算，**平均耗时降低 50%** |
| 6 | 异步消息与延迟队列 | RabbitMQ 处理支付后异步消息；**延迟队列**实现超时未接单订单自动取消 |

---

### 2.3 其他近期项目

#### 2.3.1 AI 万能视频下载总结器（SaveAny）· 前后端开发

> 基于 Vue 3 + FastAPI + yt-dlp + DeepSeek 的全栈 AI 应用，支持 1800+ 平台视频解析下载、AI 视频总结、思维导图生成、用户认证。

**项目简介**：输入一个视频链接，自动解析视频信息，支持从 B 站、YouTube、抖音等 1800+ 平台下载视频；同时提供 AI 视频总结（摘要 + 思维导图 + 问答）、字幕导出、视频直链获取等能力。

**技术栈**：Vue 3 · Vite · Tailwind CSS · Axios · FastAPI · uvicorn · Python 3.10+ · yt-dlp · ffmpeg · DeepSeek API（OpenAI 兼容）· JWT · bcrypt · SQLite

**核心亮点**：

| # | 模块 | 关键成果 |
|---|------|---------|
| 1 | 多平台视频解析下载 | 基于 yt-dlp 支持 1800+ 网站，涵盖 B 站、YouTube、抖音等；提供多种清晰度选择 |
| 2 | 抖音无水印下载 | 抖音等特殊平台开发专用解析模块，**无需 Cookie** 即可获取无水印视频 |
| 3 | AI 视频总结摘要 | 自动提取字幕 → 调用 DeepSeek 大模型 → 流式输出 Markdown 排版的总结摘要 |
| 4 | AI 思维导图生成 | 基于视频内容自动生成交互式思维导图；支持全屏、缩放拖拽、导出 PNG/SVG |
| 5 | AI 视频问答 | 用户基于视频内容自由问答，AI 根据字幕上下文给出针对性回答 |
| 6 | 多格式字幕导出 | 支持 SRT、VTT、TXT 等多种格式 |
| 7 | 视频直链获取 | 支持获取直链下载，不占服务器带宽 |

📁 项目地址：`C:\Users\evil\Desktop\project\free-video-downloader-master`

---

#### 2.3.2 Personal Planner（个人规划与复盘系统）· 前后端开发

> 本项目。基于 Spring Boot 3 + Vue 3 的个人规划与每日复盘系统，集成 MiniMax 大模型实现 AI 个人画像动态更新。

**项目简介**：打通"中长期目标 → 每日任务 → 每日复盘 → 个人画像动态更新"的全链路，支持目标管理（年/季/月/周层级 + KeyResult）、任务管理（今日视图 + 状态流转 + 子任务）、每日复盘、标签、学习记录、个人画像（手动 + AI 增量更新 + AI 完整 MD 画像生成 + 用户编辑）、数据分析（热力图、趋势）等模块。

**技术栈**：Spring Boot 3.2.7 · Spring Security 6 · MyBatis-Plus 3.5.5 · MySQL 8.0 · jjwt 0.12.6 · Vue 3.4 · TypeScript 5 · Vite 5 · Element Plus 2.7 · Pinia 2 · Vue Router 4 · Axios · ECharts 5 · dayjs · markdown-it · MiniMax M3（Anthropic 兼容 API）

**核心亮点**：

| # | 模块 | 关键成果 |
|---|------|---------|
| 1 | 完整业务闭环 | 12 张主表 + profile_document 画像文档表，涵盖目标/任务/复盘/标签/学习/画像/分析 |
| 2 | JWT 无状态认证 | 1 天 accessToken + 7 天 refreshToken；401 拦截器友好提示 |
| 3 | AI 个人画像 | 两种 AI 能力：① 基于学习记录的增量更新（评级升降）；② 上传简历/技术总结 MD → LLM 生成结构化 6 章节画像 |
| 4 | 画像文档可编辑 | LLM 生成的 MD 完整保存到数据库；前端支持预览/编辑/复制/下载；用户编辑后 PUT 保存（version +1） |
| 5 | 智能 Prompt 工程 | 12000 字符截断 + ⚠️ 重要原则（优先文档、忠实于用户自评）+ 输出模板强制结构化 |
| 6 | MiniMax M3 集成 | Anthropic 兼容协议；RestTemplate 连接 15s/读取 120s 超时；x-api-key + anthropic-version headers |
| 7 | 浅色美观 UI | MainLayout 侧边栏浅色背景 + 蓝绿渐变激活项；全局 CSS 变量系统；卡片圆角 10px |

📁 项目地址：`C:\Users\evil\Desktop\project\personal-planner`

---

## 三、掌握技能

> 综合两段实习 + 个人项目后沉淀的技能，按一级分类组织。

### 3.1 Java 后端（核心能力）

| 技能 | 评级 | 证据 |
|------|------|------|
| Java 基础 | A | 熟悉字符串、集合、反射、IO 流等核心知识 |
| Spring / Spring MVC / Spring Boot | A | 实习 + 4 个项目均使用；熟悉 IoC、AOP |
| Spring AI | A | 个人项目智能旅游助手全链路实操（对话记忆、RAG、工具调用、MCP、热更新） |
| MyBatis / MyBatis-Plus | A | 实习 + 代驾通项目大量使用 |
| Java 并发编程 | B | 线程池、CAS、CompletableFuture 实战；代驾通异步编排降低 50% 耗时 |
| 多线程异步编排 | A | CompletableFuture 实战（游记响应 -30%、结算耗时 -50%） |

### 3.2 中间件与基础设施

| 技能 | 评级 | 证据 |
|------|------|------|
| MySQL | A | 索引、锁机制、MVCC、事务隔离；航信主导 44 张表设计 |
| Redis | A | 二级缓存（Caffeine + Redis，壮游家）；Pub/Sub 多 Pod 刷新；GEO 实时位置匹配（代驾通）；限流；分布式锁（Redisson） |
| RabbitMQ | B | 异步消息、延迟队列、消息可靠性、死信队列（代驾通） |
| MongoDB | B | 轨迹持久化（代驾通） |
| Flink | B | MapFunction + 维表预加载，查询延迟 -80%（航信） |
| Docker | C | 代驾通容器化部署 |
| Nginx | C | 反向代理、负载均衡基础（壮游家） |
| 海豚调度器 / XXL-Job | B | 日频 SQL 自动化调度（航信）；司机位置定时匹配（代驾通） |

### 3.3 大数据与数据工程

| 技能 | 评级 | 证据 |
|------|------|------|
| Hadoop / Hive / Spark | C | 实习期使用过 |
| 数据分层建模 | B | ODS → DWD 完整链路（航信"掌尚飞"） |
| 数据加密与安全 | B | EnvironmentPostProcessor 启动前置解密（航信） |
| Flink 维表预加载 | B | 机场维表预加载，延迟 -80% |

### 3.4 AI / LLM 工程化

| 技能 | 评级 | 证据 |
|------|------|------|
| Spring AI | A | RAG 全链路实操（改写→检索→过滤→注入） |
| RAG 检索增强 | A | 改写 query、相似度阈值、topK、metadata 元信息注入、城市维度精准过滤 |
| Tool Calling / Function Calling | A | 智能旅游助手：联网搜索、网页抓取、PDF 生成、资源下载 |
| MCP 协议 | B | STDIO 模式集成地图服务（天气、经纬度、POI 搜索） |
| 统一 AI 服务层封装 | A | 壮游家文旅投诉智能分析：实体识别 + 多级舆情分类 + 地域归属 + 场景化建议 |
| 多模态生成 | B | DashScope 语音克隆 + Seedream 图像生成（壮游家集福卡） |
| 对话记忆管理 | B | Kryo 序列化 + 文件系统持久化 + 自动摘要压缩（智能旅游助手） |
| 向量库与热更新 | B | 爬虫定时更新向量库，无需重启服务（智能旅游助手） |

### 3.5 高并发与性能优化

| 技能 | 评级 | 证据 |
|------|------|------|
| 二级缓存架构 | A | Caffeine + Redis（壮游家 POI 缓存） |
| 限流与降级 | A | Redis 限流 + 降级兜底（壮游家集福卡） |
| 幂等 + 分布式锁 + 乐观锁 | A | 三层抢单防护（代驾通） |
| 异步并行编排 | A | CompletableFuture 实战 2 个项目 |

### 3.6 前端

| 技能 | 评级 | 证据 |
|------|------|------|
| Vue 3 | B | 智能旅游助手前端开发（Composition API） |
| 前后端分离开发 | B | 能独立完成 CRUD 与联调 |

### 3.7 Vibe Coding（AI 辅助开发）

| 技能 | 评级 | 证据 |
|------|------|------|
| Claude Code | A | 日常工作主力，覆盖新需求实现、调试重构、方案设计 |
| 工具链 | B | 熟悉 Git、Docker 基本使用 |

---

## 四、需要补齐的能力

### P0 — 必须补齐（影响职级跃迁）

| 优先级 | 能力 | 当前问题 | 补齐方向 |
|--------|------|---------|---------|
| P0 | 系统设计 / 技术方案 | 无法很好设计系统，给出技术方案 | 系统学习《系统设计面试》《凤凰架构》；每业务场景先写方案再写代码 |
| P0 | Agent / Workflow / LLM 原理 | 了解不全面，缺乏实战经验 | ReAct、Plan-and-Execute、Multi-Agent；Transformer 原理；用 Java 手写简易 Agent 循环 |
| P0 | 设计支持高并发的 Agent 系统 | 优秀 Agent 系统应具备的能力了解不全面；Agent 系统如何支持高并发 | 学习 RAIO/PyRAG 架构；研究流式输出、多路竞争、限流降级在 Agent 中的应用 |

### P1 — 应该补齐（扩展能力边界）

| 优先级 | 能力 | 当前问题 | 补齐方向 |
|--------|------|---------|---------|
| P1 | 全栈能力深化（CSS 布局、复杂交互） | 只了解 Vue 基本内容，CSS/HTML 基础薄弱，开发靠 AI 工具 | 系统学 CSS（Flexbox/Grid/响应式）；做一个复杂前端场景（拖拽表单/虚拟滚动） |
| P1 | ERP / 电商领域知识 | 不了解 | 读电商系统设计案例；理解 SKU/SPU、库存扣减、订单状态机、优惠券叠加 |
| P1 | AI 编程工具能力 | 主要用 Claude Code，没深入学习；Codex 没用过 | 深入学习 Claude Code 高级特性（Hook、SubAgent、Plan Mode）；试用 Codex/Cursor 对比 |

---

> 文档版本：v1.0（2026-06-14 重构）
> 结构调整：4 大板块（实习 / 个人项目 / 掌握技能 / 未掌握技能），技能部分整合实习与项目证据
